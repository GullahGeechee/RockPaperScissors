const playerChoice = document.createElement('h1')
const cpuChoice = document.createElement('h1')
const resultPlayer = document.createElement('h1')
const gameGrid = document.getElementById('game')
gameGrid.append(playerChoice, cpuChoice, resultPlayer)
//Checked via Live-server successful

const choices = ['paper', 'scissors', 'rock']
// declaring choices


